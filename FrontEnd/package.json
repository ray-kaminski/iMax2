{
  "name": "imax",
  "version": "1.0.0",
  "description": "iMax the screen project to end all screen projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "karma": "~0.12",
    "karma-chrome-launcher": "^0.1.12",
    "karma-firefox-launcher": "^0.1.6",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2",
    "protractor": "^2.1.0"
  },
  "scripts": {
    "start": "http-server ./app -a localhost -p 8000 -c-1",
    "postinstall" : "bower install"
  }
}